# ============================================================
# JUJU DRIFT - CLIENT ENVIRONMENT VARIABLES
# ============================================================
# IMPORTANTE: Este arquivo serve como template para:
#   - .env.development (desenvolvimento local)
#   - .env.production (build de produção)
#   - .env.staging (ambiente de testes)
#
# Copie e renomeie conforme necessário
# ============================================================

# ------------------------------------------------------------
# BACKEND CONNECTIONS
# ------------------------------------------------------------
# WebSocket URL do servidor Colyseus
# Desenvolvimento: ws://localhost:2567
# Produção: wss://game-server.seu-dominio.com
VITE_WS_URL=ws://localhost:2567

# REST API Backend URL
# Desenvolvimento: http://localhost:2567
# Produção: https://api.seu-dominio.com
VITE_API_URL=http://localhost:2567

# ------------------------------------------------------------
# APPLICATION SETTINGS
# ------------------------------------------------------------
# Ambiente (usado para features condicionais)
NODE_ENV=development  # production | development | staging

# Nome da aplicação (exibido no título da página)
VITE_APP_NAME=Juju Drift

# Versão da build (opcional, para cache busting)
VITE_APP_VERSION=1.0.0

# ------------------------------------------------------------
# UPLOAD & CDN
# ------------------------------------------------------------
# URL base para assets estáticos (logos, imagens)
# Desenvolvimento: /assets (local)
# Produção: https://cdn.seu-dominio.com (CloudFront, Cloudflare, etc)
VITE_CDN_URL=/assets

# URL do logo do app (pode ser sobrescrito por API)
VITE_LOGO_URL=/assets/logo.png

# ------------------------------------------------------------
# ANALYTICS & MONITORING (OPCIONAL)
# ------------------------------------------------------------
# Google Analytics
VITE_GA_TRACKING_ID=

# Sentry (frontend error tracking)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development

# Hotjar (heatmaps & analytics)
VITE_HOTJAR_ID=
VITE_HOTJAR_VERSION=6

# ------------------------------------------------------------
# FEATURE FLAGS
# ------------------------------------------------------------
# Habilitar/desabilitar features experimentais
VITE_ENABLE_AFFILIATES=true
VITE_ENABLE_PAYMENTS=true
VITE_ENABLE_CHAT=false
VITE_ENABLE_LEADERBOARD=true

# Mostrar botões de debug (apenas dev)
VITE_SHOW_DEBUG_CONTROLS=false

# ------------------------------------------------------------
# GAME SETTINGS (OPCIONAL - podem ser sobrescritos pela API)
# ------------------------------------------------------------
# FPS alvo do Babylon.js
VITE_TARGET_FPS=60

# Qualidade gráfica padrão (low | medium | high)
VITE_DEFAULT_GRAPHICS_QUALITY=high

# Habilitar anti-aliasing
VITE_ENABLE_ANTIALIASING=true

# ------------------------------------------------------------
# PAYMENT & PIX (FRONTEND)
# ------------------------------------------------------------
# Mercado Pago Public Key (para checkout frontend)
VITE_MP_PUBLIC_KEY=your_mercadopago_public_key

# Valores mínimo/máximo para depósitos (em R$)
VITE_MIN_DEPOSIT=10
VITE_MAX_DEPOSIT=10000

# Valores mínimo/máximo para saques (em R$)
VITE_MIN_WITHDRAWAL=20
VITE_MAX_WITHDRAWAL=50000

# ------------------------------------------------------------
# SOCIAL & SEO
# ------------------------------------------------------------
# URLs das redes sociais (footer links)
VITE_SOCIAL_INSTAGRAM=https://instagram.com/driftcash
VITE_SOCIAL_TWITTER=https://twitter.com/driftcash
VITE_SOCIAL_DISCORD=https://discord.gg/driftcash
VITE_SOCIAL_TELEGRAM=https://t.me/driftcash

# Meta tags para SEO (caso não venham da API)
VITE_SEO_TITLE=Juju Drift - Multiplayer Racing Game
VITE_SEO_DESCRIPTION=Jogue drift multiplayer online e ganhe prêmios reais!
VITE_SEO_KEYWORDS=drift,racing,multiplayer,online,game,pix,brasil

# ------------------------------------------------------------
# EXEMPLOS DE CONFIGURAÇÕES POR AMBIENTE
# ------------------------------------------------------------

# --- .env.development ---
# VITE_WS_URL=ws://localhost:2567
# VITE_API_URL=http://localhost:2567
# NODE_ENV=development
# VITE_SHOW_DEBUG_CONTROLS=true

# --- .env.production ---
# VITE_WS_URL=wss://game-server.driftcash.com
# VITE_API_URL=https://api.driftcash.com
# NODE_ENV=production
# VITE_CDN_URL=https://cdn.driftcash.com
# VITE_GA_TRACKING_ID=G-XXXXXXXXXX
# VITE_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx
# VITE_SHOW_DEBUG_CONTROLS=false

# --- .env.staging ---
# VITE_WS_URL=wss://staging-game.driftcash.com
# VITE_API_URL=https://staging-api.driftcash.com
# NODE_ENV=staging
# VITE_SHOW_DEBUG_CONTROLS=true

# ------------------------------------------------------------
# NOTAS DE SEGURANÇA
# ------------------------------------------------------------
# ⚠️  NUNCA coloque secrets/tokens sensíveis aqui!
# ⚠️  Todas variáveis com prefixo VITE_ ficam EXPOSTAS no bundle final
# ⚠️  Para secrets, use apenas no backend e exponha via API se necessário
# ------------------------------------------------------------
